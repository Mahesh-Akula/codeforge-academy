This pull request adds the Testing category (Functional / Manual Tester) to the repository.

Changes:
- Added src/data/categories.js with the Testing category object

Notes:
- If the project already has an existing categories file/array, consider merging this entry into it rather than keeping a separate file.
